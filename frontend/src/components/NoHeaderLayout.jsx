// src/layouts/NoHeaderLayout.jsx
import React from 'react';

const NoHeaderLayout = ({ children }) => {
  return <>{children}</>; // Just render the children (no header)
};

export default NoHeaderLayout;
